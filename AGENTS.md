# AGENTS.md — 에이전트 진입점 (marketing)

> 어느 개발 환경에서 열든 가장 먼저 이 파일을 읽으세요.
> `CLAUDE.md`는 회사 컨텍스트와 마케팅 원칙을 담고 있으며, 이 파일은 작업 진입점입니다.

## 0. 먼저 읽을 순서
1. **§0-A 모델 라우팅 지침**(바로 아래) — 어느 모델이 무엇을 맡는지의 정본.
2. **CLAUDE.md** (리포 루트) — 회사 컨텍스트·마케팅 원칙.
3. 작업할 채널의 단일 인계서 — BAND는 **HANDOFF-BAND.md**, Instagram은 **HANDOFF-INSTAGRAM.md**,
   foresttour.kr 블로그는 **HANDOFF-BLOG.md**, 네이버 블로그는 **HANDOFF-NAVER-BLOG.md**.
4. 상세 이력이 필요한 경우에만 **LOG.md** 맨 아래 최신 "세션 핸드오프" 블록.
5. 작업에 직접 관련된 하위 문서만 읽는다. 전체 `learning/`을 선행 로드하지 않는다.

## 0-A. 모델 교차검증·폴백 정책 (사장 지시 2026-07-29)
> **실행자 기본값 갱신(전역 개정 2026-08-24 준용)**: 조사·단순 구현·문서 초안 등 일반 작업은
> agy `gemini-3.7-flash-high`(ACP) 우선 위임 — 위임 전 고위험 사전판정·민감정보 최소화, 수행 중
> 고위험 결합 시 즉시 증단 반환, 쿼터는 세션 차단 fail-fast. 아래 문구 중 이와 충돌하는 실행자
> 지정은 본 줄이 우선하며, 설계 확정·고위험 판정·검증 강도는 기존 유지다.

이 절은 `CLAUDE.md`의 기존 모델 역할 분담보다 최신인 운영 지시다. 에이전트 제품마다 모델명과
호출 가능 범위가 다르므로, **호출하지 못한 모델을 호출했다고 기록하거나 다른 모델을 같은 모델인
것처럼 가장하지 않는다.** 현재 세션에서 지원하지 않는 모델은 명시된 다음 폴백으로 넘기고,
실제로 사용한 생성·검증 모델과 한계는 결과에 기록한다.

1. **기본 초안·설계·판단**
   - 기본: **GPT-5.6 Luna max**
   - 절차: Luna가 초안을 판단 → **GPT-5.6 Sol medium이 모든 실질 답변을 독립 검증·반박**
     → Luna가 반박을 수용·기각하고 근거를 남김 → 최종 확정.
2. **저추론·쓰기·웹검색·구현**
   - 기본: **AGY Gemini 3.6 Flash low**
   - 폴백: **GPT-5.6 Luna max**
   - AGY 수행 결과도 Sol medium의 독립 검토를 거친 뒤 확정한다.
3. **사용자 판단이 필요해 보이는 영역**
   - 곧바로 사용자에게 묻지 않는다. 먼저 **Luna max → GPT-5.6 Sol medium**이
     각각 진행 가능성·위험·되돌릴 수 있는 대안을 판단하고 검증·반박·수용한다.
   - Sol 호출 불가 시 독립 검증 실패를 명시하고, 비가역 변경은 확정하지 않는다.
   - 두 판단을 거쳐도 권한·외부 인증·비가역 영향·핵심 선택 때문에 안전하게 진행할 수 없을 때만
     사용자에게 필요한 최소 질문을 한다.
4. **검증 기록**
   - 산출물 또는 운영 로그에 `수행 모델`, `검증 모델`, `주요 반박`, `최종 수용/기각`,
     `폴백 사유`를 간결하게 남긴다.
   - API·UI·파일·git 접근이 없는 순수 텍스트 모델의 보고는 증거가 아니다. 담당 에이전트가
     실제 파일, 테스트, 브라우저 화면 또는 공식 원문으로 재검증한다.

## 0-B. 브라우저 감사 규칙

- 브라우저 확인은 운영자가 로그인한 브라우저에서 직접 수행하거나 현재 제공되는 일반 브라우저
  도구만 사용한다. 전용 자동화 런너나 별도 빌드에 의존하지 않는다.
- 변경 뒤에는 새 화면과 공개 URL에서 결과를 재검증한다.
- 로그인 만료·2차 인증·CAPTCHA·권한 부족은 우회하지 않는다.

## 1. 도구·에이전트 차이

- 현재 환경에 실제로 노출된 도구만 사용하고, 없는 MCP·모델·UI 기능을 썼다고 기록하지 않는다.
- Gmail·Drive·Calendar 작업은 운영 Google 계정, GitHub 작업은 조직 접근 계정의 인증이 필요하다.
  인증이 없으면 우회하지 말고 로컬에서 가능한 작업까지만 수행한다.
- 외부 메시지는 초안까지만 만들며, 명시적 발송 요청 없이는 보내지 않는다.
