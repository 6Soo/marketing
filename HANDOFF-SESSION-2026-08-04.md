# 세션 핸드오프 — 2026-08-04

> 범위: 2026-07-29 시작 BAND 전면 인계·자동 실행 세션의 최종 정리.
> 다음 세션은 `AGENTS.md` → `CLAUDE.md` → `HANDOFF-BAND.md` → 이 문서 순으로 읽는다.

## 1. 세션 결과

- BAND 관련 전략·운영·OG·도구 자료를 `HANDOFF-BAND.md` 한 문서로 통합했다.
- `AGENTS.md`에 모델 정책의 실제 정본 위치인 §0-A와 WSL 브라우저 실행 규칙 §0-B를 반영했다.
- WSL의 Orca computer-use CLI가 로그인된 Windows Edge BAND 탭을 읽고 조작하는 경로를 실증했다.
- BAND `post/1425`를 게시 약 5시간 뒤 직접 열어 `0 읽음 / 댓글 0 / 반응 0`을 확인했다.
- Fable 5 medium의 “게시 실패·비공개 가능성을 먼저 배제하라”는 반박을 수용해, WSL의 쿠키 없는
  HTTP 요청으로 공개 HTML의 제목·설명·canonical URL을 확인했다. 게시 실패·비공개 가설은 기각했다.
- 최신 피드에서 일정 자동 공유글 최소 6건 연속, 확인 가능한 읽음 1~2를 관측했다. 현 병목을
  개별 질문 문구보다 피드 자체의 도달과 자동 공유 점유로 유지했다.
- `tools/band-publish.mjs`가 Gemini 오류 시 카드 원문을 성공 초안처럼 저장하던 동작을 제거했다.
  오류·빈 응답은 fail-closed로 실패하며, 출력에도 반자동 검토 대상임을 명시한다.
- `tools/band-publish.test.mjs`와 `npm run test:band`를 추가했다. 테스트 2개가 통과했다.

## 2. 공개 변경과 검증

### BAND 공개 쓰기

- 글: `https://band.us/band/56995933/post/1425`
- 게시 시각: 2026-07-29 17:46 KST
- 게시판: `#질문·동행_찾기`
- 가격·상품 링크·예약 CTA·PII 없음
- 이 세션에서 기존 일정 수정·공유글 삭제·추가 질문 게시는 하지 않았다.

### Git

- 최초 커밋: `64ba786` (`docs(band): consolidate handoff and fail closed`)
- 원격 `main` 선행 변경을 강제 덮어쓰지 않고 `origin/main` 위로 rebase했다.
- 최종 커밋: `a09163b`
- 원격 반영: `origin/main`에 push 완료
- rebase 전후 `package-lock.json` SHA-256:
  `629c755e1c310889be30c57442cc16dd843ae2190bc3c76d563812a8db581526`
  — 사용자 변경이 그대로 보존됐다.

## 3. 모델 기록

- 수행·최종 판단: GPT-5.6 Sol medium
- 반대 검증: Claude Fable 5 medium
- Fable 주요 반박: `0 읽음`을 저반응으로 해석하기 전에 게시 공개성·측정 경로를 확인해야 한다.
- 최종 수용: 비로그인 공개 응답을 즉시 확인하는 절차를 추가했다.
- 최종 기각: 공개 HTML과 로그인 UI가 동일 글을 반환하므로 게시 실패 가설은 기각했다.
- 저추론 기본 AGY Gemini 3.6 Flash high: `GEMINI_API_KEY` 부재로 호출 실패.
- AGY Sonnet 4.6 폴백: 호출 결과 본문을 받지 못해 증거로 사용하지 않았다.
- 현재 2026-08-04 재개 세션은 Orca 워커 규칙에 따라 다른 모델로 재위임하지 않고 문서화만 수행했다.

## 4. 다음 세션의 정확한 시작점

### P0 — 누락된 체크포인트 현행화

- 예정됐던 24h 시각: 2026-07-30 17:46 KST
- 예정됐던 72h 시각: 2026-08-01 17:46 KST
- 2026-08-04 인계 시점 상태: **관측값 미수집**
- BAND 읽음은 현재 누적값이므로 지금 조회한 값을 과거 24h·72h 값으로 소급 기록하지 않는다.
- 다음 세션은 현재 누적 읽음·댓글·반응을 `late_current`로 별도 기록하고, 과거 체크포인트는
  `missed`로 표시한다.

### P1 — 현재 피드 기준선

- 최근 20건을 일정 자동 공유 / 상품형 / 여행지·후기 / 관계형 / 운영공지로 분류한다.
- 공개 ID·시각·현재 읽음·댓글·반응 숫자만 남기고 작성자·댓글 원문은 기록하지 않는다.
- `post/1425`가 여전히 읽음 0~2면 동일 질문을 반복하지 않는다.

### P2 — BAND→사이트 계측

- `/admin/traffic`에서 `from=band`, BAND 리퍼러를 기간별로 확인한다.
- BAND 글 읽음 자체가 없으면 피드 도달 문제, 읽음은 있으나 방문이 없으면 링크·OG·CTA 문제로 분리한다.

### P3 — 신규 일정에만 공유 끄기 파일럿

- 기존 일정을 수정해 파일럿을 만들지 않는다.
- 다음 실제 신규 일정 1건에서만 출발일 하루, 전체 여행기간 본문, `게시글로 공유` 끔을 적용한다.
- 저장 후 신규 피드 공유글 0건을 확인한다.

## 5. 금지·함정

- 과거 후보 수 `61/9/52`를 현재 미등록 수로 사용하지 않는다. 추가 11건은 UI에서 모두 확인됐다.
- 기존 다일 일정을 일괄 축소하지 않는다.
- 일정과 공유글 결합 관계가 불명한 상태에서 기존 공유글을 삭제하지 않는다.
- 체크포인트를 지각 조회한 누적값으로 소급 생성하지 않는다.
- `tools/band-publish.mjs`는 공개 게시기가 아니라 반자동 초안 생성기다.
- 현재 워킹트리의 `package-lock.json` 수정은 이 세션 소유가 아니다. 되돌리거나 커밋에 섞지 않는다.

