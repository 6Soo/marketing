.env
# .env 사본·백업도 전부 제외 (.env.bak-*, .env.local 등 — 실제 유출 사고 방지 2026-07-29)
.env.*
!.env.example

# 인스타 발행 스테이징(전송 산출물, 커밋 안 함)
cardnews/out/_publish/
_stage/
data/loop-state/
*.instagram-token
/bus/
node_modules/

# Local agent/runtime state; never part of marketing artifacts.
.omc/
.codex-remote-attachments/

# cycle.sh 작업 산출물(설계·반박·최종). 확정본은 대상 리포 docs/로 옮겨 보존한다.
.cycle/
