# 세션 인계 — 2026-07-29

> 이 채팅 세션이 한 프로젝트 작업 전부를 정리한 문서다. **새 세션은 이것부터 읽는다.**
> 상세 근거·실측치는 `LOG.md`에 있고, 이 문서는 그 지도 역할이다.
>
> **범위에서 제외**: 오케스트레이션 러너(`~/.orca/bin/cycle.sh`) 관련 작업은 이 프로젝트 담당이
> 아니다(사장 지시 2026-07-29). `LOG.md` (H)/(H-1)/(H-2)/(H-3) 절이 그 기록이며, **새 세션은
> 그 절을 이어받지 않는다.** 다만 그 러너가 만들어낸 산출물(아래 §4 CTA 관측성 브랜치)은
> 프로젝트 자산이므로 처리 대상에 남는다.

---

## 1. 시작할 때 확인할 것

```
git -C <각 리포> pull        # 상시 공유 규칙: 작업 시작 시 pull 필수
```

리포별 최종 상태(이 문서 작성 시점):

| 리포 | 경로 | 브랜치 | 최종 커밋 | 원격 |
|---|---|---|---|---|
| marketing | `C:/Users/kkokk/orca/workspaces/marketing/foresttour.kr` | `6Soo/foresttour.kr` | `e4701a4` | 푸시됨 (`main`도 동일) |
| reservation | `D:/OneDrive/문서/AX/reservation` | `main` | `ee1bcdf` | 푸시됨 (프로덕션 배포 완료) |
| recruit | `D:/OneDrive/문서/AX/recruit` | `main` | `b7a6b8f` | 푸시됨 |
| foresttour(전사) | `D:/OneDrive/문서/AX/foresttour` | `main` | `70ad447` | 푸시됨 |
| reservation(CTA 작업분) | `C:/Users/kkokk/orca/workspaces/reservation/cta-observability` | `6Soo/cta-observability` | `9e79b69` | **미푸시·미병합** |

⚠ reservation 워킹트리에 **다른 세션의 미완성 작업**이 있다(`src/app/api/upload/route.ts`,
`src/lib/reservationDetails.ts`, `src/lib/uploadImage.ts`, `src/app/api/reservation-access/[token]/passport/`).
**커밋에 섞지 마라.** `tests/reservationDetailsApi.test.ts` 1건 실패는 그 작업 때문이며
내 변경 이전부터 실패했다(stash 대조로 확인). 내 작업의 회귀가 아니다.

---

## 2. 이번 세션에서 **확정된 원칙** (CLAUDE.md에 이미 반영됨)

### 2-1. 전나무 = (주)숲길여행 공식 로고 (사장 확정 2026-07-29)

- **단일 원천**: 전사 리포 `brand/forest-tour-fir.svg` · 사양 `brand/README.md`
- marketing 리포 사본: `cardnews/brand-forest-tour-fir.svg`
- reservation 리포 좌표 원천: `src/lib/brandFir.ts`
- **형상을 다시 그리지 않는다.** 잎사귀 픽토그램·🌲 이모지·추상 산 기호로 대체 금지
  (실제로 세 지면에서 그렇게 흩어져 있었고 이번에 정리했다)
- 단색으로 쓸 때는 색 지정 금지, `currentColor`
- 최소 16px(실루엣만 남음) · 18px부터 단 구분 · **권장 24px↑**

### 2-2. 카드뉴스 비주얼 계약 갱신

좌상 워터마크 = **공식 로고(전나무) + '숲길따라 감성여행' 텍스트**.
기준본(`cardnews/나만몰랐던일본-001-히다.html`)을 먼저 고치고 `cardnews/tools/template.mjs`로
옮기는 **단방향 규칙은 그대로**다. 역방향 금지.

### 2-3. 그 밖에 이 세션 이전부터 유효한 것

`CLAUDE.md`의 제0원칙(여행상품이 아니라 여행지), 사진 소싱 화면 기준 우선(2026-07-28 개정),
카페·밴드 회원을 인스타로 끌어오지 않는다(2026-07-29) — 전부 그대로다.

---

## 3. 이번 세션 산출물 (전부 푸시됨)

### marketing
| 커밋 | 내용 |
|---|---|
| `150430b` | 브랜드 심볼 V5 기록 (reservation `351d801` 대응) |
| `3de8aa0` | 카드뉴스 좌상 워터마크에 공식 로고 삽입 (기준본 → 템플릿) |
| `e4701a4` | 공식 로고 확정 — 비주얼 계약 조항 + 전 리포 전달 기록 |

### reservation (프로덕션 배포 완료)
| 커밋 | 내용 |
|---|---|
| `351d801` | 헤더 좌상단 스탬프를 V4 추상 기호 → 전나무 단색으로 교체 |
| `b29d2e9` | 개별 여행 OG/트위터 카드 마크(원형 점) → 전나무, 좌표를 `src/lib/brandFir.ts`로 단일화 |
| `ee1bcdf` | 위 작업 + 다른 세션의 홈 공유 썸네일 밝기 보정 병합 |

### recruit
| 커밋 | 내용 |
|---|---|
| `b7a6b8f` | 잎사귀 픽토그램 2곳·🌲 이모지 파비콘 1곳 → 공식 로고, 헤더 아이콘 16→18px |

### foresttour (전사)
| 커밋 | 내용 |
|---|---|
| `d2a684b` | `brand/` 단일 원천 신설 (SVG + 사양서) |
| `70ad447` | CLAUDE.md 확정 의사결정에 공식 로고 조항 |

---

## 4. 미해결 — 다음 세션이 판단·처리할 것

우선순위 순.

### (1) 인스타 노출이 0이다 — 사장님 판단이 필요한 지점 ★
게시물 5건 전부 reach 1(본인 조회로 봐야 함), 저장·좋아요·댓글·공유 0, 팔로워 0.
**병목은 파이프라인도 콘텐츠 품질도 아니라 노출 자체가 없다는 것이다.** 팔로워 0·해시태그
유입 0에서 게시만 반복하면 측정 루프가 학습할 게 없다. 자동화를 더 조이기 전에 **최초 도달을
만드는 수단**을 먼저 다뤄야 한다. 근거는 `LOG.md` "활성화 실태 (냉정하게)" 절.
※ 카페·밴드 회원을 인스타 팔로워로 끌어오는 것은 **금지**다(알고리즘 이유, CLAUDE.md 참조).

### (2) CTA 관측성 브랜치 처리 — `6Soo/cta-observability` @ `9e79b69`
- 내용: `src/lib/storyCtaStatus.ts` 신설(스토리 CTA 상태 판정 + 36시간 staleness),
  `storyTours.ts`·`api/health`·`api/sync-tours`·`scripts/smoke.mjs` 수정, 테스트 2종 추가
- 상태: **테스트 94/94 · tsc 0 · ESLint 0 · build 성공**은 확인됐으나 **독립 검증 판정을 받지 못했다**
- 핵심 설계 근거: `/api/tours`는 DB 조회와 비상 스크래핑이 **모두 실패해도 HTTP 200에 빈 tours를
  반환**한다 → 장애가 `no-candidate`로 오보된다. 그래서 envelope를 분리 파싱한다.
  원문은 브랜치의 `docs/CTA_IMPLEMENTATION_PLAN_REBUTTAL_2026-07-29.md`
- **판단 필요**: 병합·배포할지. reservation 푸시는 곧 프로덕션 배포다

### (3) 사도 공개 인스타 캡션의 잘못된 크레딧
공개 게시물 캡션이 `Tensaibuta`를 크레딧하는데 **그 사진은 게시물에 없다**. 로컬
`cardnews/series/sado/캡션.md`는 이미 교정됨. 공개 캡션 편집은 수동 작업이다.

### (4) 이미 게시된 카드뉴스에는 로고가 없다
`cardnews/out/`은 **게시 증거 파일**이라 재렌더로 덮지 않았다. 기존 카드까지 맞추려면
재렌더가 필요한데 지문(fingerprint)이 바뀐다. **다음 시리즈부터 자동 적용**되므로 급하지 않다.
맞출지 여부는 사장님 판단.

### (5) 외부 차단 — 이 세션이 풀 수 없음
- `FORESTTOUR_ADMIN_KEY` 미등록
- `ACTIVATION_COLLECT_ENABLED` 미설정
- → 24h 활성화 체크포인트가 계속 `due`

### (6) 24h 체크포인트는 사도·북알프스 모두 영구 누락
Meta 차단 기간과 겹쳤고 Instagram 인사이트는 **누적 lifetime 값**이라 지각 수집이 불가능하다.
복구 불가. 다음 유효 창은 **72h**다.

---

## 5. 함정 목록 — 같은 실수 반복 방지

이번 세션에서 실제로 밟았거나 밟을 뻔한 것들이다.

1. **`cardnews/out/`에 렌더하지 마라.** 게시 증거다. 미리보기 렌더가 `hida/cover-a.png`를
   덮어써서 즉시 복원했다. 확인이 필요하면 `cardnews/series/_임시이름`으로 복사해 렌더하고
   끝나면 지운다. ⚠ `render.mjs`가 `repoRoot = resolve(dir,'..','..','..')`로 계산하므로
   **반드시 리포 루트 기준 깊이 3**이어야 한다(`cardnews/series/<이름>`).
   또한 Chrome 스크린샷은 **비동기로 기록**되므로 렌더 직후 곧바로 읽으면 "파일 없음"이 난다.
2. **게시 증거는 줄바꿈이 바뀌면 지문이 깨진다.** `.gitattributes`가 `cardnews/out/**`,
   `cardnews/photos/**`, `data/publish-records/**`를 `-text`로 막고 있다. 풀지 마라.
3. **`--home-path=/home2` 같은 인자는 Git Bash에서 경로로 변환된다**
   (`http://localhost:3100C:/Program Files/Git/home2`가 됐다). 검증기는 PowerShell로 실행하라.
4. **reservation 워킹트리의 다른 세션 작업을 커밋에 섞지 마라**(§1 경고 참조).
5. **다른 에이전트·도구의 보고를 그대로 믿지 마라.** 이번에 "치명적 결함"이라는 반박이
   경로 문자열을 잘못 읽은 것이었고, path 전개로 반증했다.
6. **`.omc/`, `.codex-remote-attachments/`는 비소유다.** 절대 커밋·전송 금지.
7. **git 히스토리에 Pexels 키가 남아 있다**(`bfb998c`). 리포를 외부에 열기 전 재발급 필수.
8. **PII 금지** — 회원 실명·연락처·주민번호는 이 리포에 절대 기재하지 않는다.

---

## 6. 이번 세션이 확립한 작업 방식 (계속 쓸 것)

- **형상·수치는 눈대중하지 않는다.** 로고를 옮길 때 비슷하게 그리지 않고 원본 PNG를 행 단위로
  스캔해 꼭짓점을 읽었고, 렌더 대조로 **불일치 0.239%**를 확인했다. 최소 크기도 추정하지 않고
  16/18/20/24px를 실제로 렌더해 판단했다.
- **원본을 다듬지 않는다.** 전나무는 단마다 기울기가 다르다(0.636/0.757/0.992). 고르게 정리하고
  싶어지지만 고치는 순간 이미 배포된 파비콘과 다른 나무가 된다.
- **계약은 구현과 독립적으로 적어둔다.** `scripts/verify-foresttour-brand.mjs`의 `FIR_D`는
  일부러 `src/lib/brandFir.ts`를 import하지 않는다. 그래야 오타를 잡는다.
- **배포 후 프로덕션에서 실물 확인.** 로컬 통과로 끝내지 않았다.

---

## 7. 이 문서 갱신 규칙

세션이 끝날 때 §1 표(커밋·브랜치)와 §4(미해결)를 갱신한다. 상세 근거는 `LOG.md`에 추가하고
이 문서에는 요약과 포인터만 남긴다. 길어지면 인계 문서로서의 값이 떨어진다.
